# Bunny Rabbit — apps/web (placeholders only; no real secrets)

# --- NEXT_PUBLIC_* (safe for browser; set in Vercel Preview first) ---
# Canonical URL for the deployment (Preview or Production)
NEXT_PUBLIC_APP_URL=https://bunny-rabbit-git-preview-stud-ex-s-projects.vercel.app
# Optional: environment label shown in future fleet tooling
# NEXT_PUBLIC_STUDEX_ENV=preview
# Optional analytics
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# --- BUZZ_* (optional Studex agent / gateway hooks — server or build-time only) ---
# BUZZ_GATEWAY_URL=https://buzz-gateway.example.studex.internal
# BUZZ_API_KEY=your-buzz-api-key-placeholder
# BUZZ_SERVICE_ID=bunny-rabbit-travel
# BUZZ_LITELLM_BASE_URL=http://localhost:4000

# --- Local dev (apps/web) ---
PORT=3000

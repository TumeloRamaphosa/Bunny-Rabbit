{
  "name": "@bunny-rabbit/edge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist 2>/dev/null || node ./scripts/build-stub.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250917.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.37.1"
  }
}
